apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.gateway.namespace | default "mcp-shared" }}
  labels:
    app.kubernetes.io/name: {{ .Values.gateway.name }}
    app.kubernetes.io/component: mcp-infrastructure

